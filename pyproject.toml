[project]
name = "mazegen"
version = "1.0.0"
description = "Generates and solves a perfect maze."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "flake8>=7.3.0",
  "mypy>=1.19.1",
  "numpy>=2.4.3",
  "ruff>=0.15.5",
  "setuptools>=82.0.1",
  "ty>=0.0.21",
]
